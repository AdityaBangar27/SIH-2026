<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardWorksheetRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="14dp"
    app:cardBackgroundColor="?attr/customCardColor"
    app:cardCornerRadius="16dp"
    app:cardElevation="1dp"
    app:strokeColor="?attr/customBorderColor"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Title and Subtitle -->
        <TextView
            android:id="@+id/tvWorksheetTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/worksheet_numbers_title"
            android:textColor="?attr/customTextColor"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvWorksheetSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="@string/worksheet_numbers_sub"
            android:textColor="?attr/customTextSecondaryColor"
            android:textSize="12sp" />

        <!-- Educational Preview Box -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="10dp"
            app:cardBackgroundColor="?attr/customCardTintColor"
            app:cardCornerRadius="10dp"
            app:cardElevation="0dp"
            app:strokeColor="?attr/customBorderColor"
            app:strokeWidth="1dp">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <!-- Image preview for Numbers -->
                <ImageView
                    android:id="@+id/ivWorksheetPreview"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="Worksheet Preview"
                    android:padding="6dp"
                    android:scaleType="fitCenter"
                    android:src="@drawable/ic_apples_three" />

                <!-- Text preview for Formulas -->
                <TextView
                    android:id="@+id/tvWorksheetMathPreview"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:letterSpacing="0.1"
                    android:text="2 + 3 = ?"
                    android:textColor="?attr/customPrimaryColor"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:visibility="gone" />
            </FrameLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Download Progress Bar (visible during simulated download) -->
        <ProgressBar
            android:id="@+id/pbWorksheetDownload"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_marginTop="10dp"
            android:indeterminate="false"
            android:max="100"
            android:progress="45"
            android:progressTint="?attr/customPrimaryColor"
            android:visibility="gone" />

        <!-- Download PDF Action Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDownloadPdf"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginTop="12dp"
            android:backgroundTint="?attr/customPrimaryColor"
            android:text="@string/btn_download_pdf"
            android:textAllCaps="false"
            android:textColor="?attr/colorOnPrimary"
            android:textSize="13sp"
            android:textStyle="bold"
            app:cornerRadius="22dp"
            app:icon="@drawable/ic_pdf"
            app:iconGravity="textStart"
            app:iconSize="18dp"
            app:iconTint="?attr/colorOnPrimary" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
